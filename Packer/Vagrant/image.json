{
    "builder": [
        {
            "type": "virtualbox",
            "source_partM": "ubuntu.box",
            "ssh_username": "ubuntu",
            "ssh_password": "ubuntu",
            "shutdown_command": "echo 'packer' | sudo -S shutdown -P now"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "script": "install.sh"
        }
    ],
    "post-processor": ["vagrant"]
}